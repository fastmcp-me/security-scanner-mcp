// Simple script to ensure patterns are available in dist
import { writeFileSync } from 'fs';
import { join } from 'path';

// This is a placeholder - in a real implementation, you might copy pattern files
// For now, patterns are embedded in the scanner.ts file
console.log('Build complete');